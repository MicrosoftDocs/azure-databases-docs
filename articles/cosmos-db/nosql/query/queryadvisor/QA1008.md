---
title: QA1008 - GetCurrentTicks
titleSuffix: Azure Cosmos DB for NoSQL
description: The query uses GetCurrentTicks in the WHERE clause, which doesn't utilize index and can cause higher RU charge.
author: jcodella
ms.author: jacodel
ms.service: azure-cosmos-db
ms.subservice: nosql
ms.topic: reference
ms.devlang: nosql
ms.date: 11/04/2025
---

# QA1008: GetCurrentTicks

| Property      | Value            |
|---------------|------------------|
| RuleID        | QA1008           |
| Title         | GetCurrentTicks  |
| Category      | Performance      |

## Cause
The query uses `GetCurrentTicks` in the WHERE clause.

## Rule Description
 The function `GetCurrentTicks` doesn't utilize index and can cause higher RU charge and slower query performance. 

## Recommendation
Consider using `GetCurrentTicksStatic` instead, which utilizes index and is deterministic. For more information on `GetCurrentTicksStatic` please see [GetCurrentTicksStatic - Azure Cosmos DB for NoSQL | Microsoft Learn](../getcurrentticksstatic.md).

## Example
Original query:

```sql
SELECT * 
FROM c 
WHERE c.timestamp > GetCurrentTicks()
```

Revised query:

```sql
SELECT * 
FROM c 
WHERE c.timestamp > GetCurrentTicksStatic
```